import { UUID } from "../types/UUID";
export default class Node {
    private _id;
    private _uuid;
    private _public;
    private _name;
    private _description;
    private _location_id;
    private _fqdn;
    private _scheme;
    private _behind_proxy;
    private _memory;
    private _memory_overallocate;
    private _disk;
    private _disk_overallocate;
    private _upload_size;
    private _daemon_listen;
    private _daemon_sftp;
    private _daemon_base;
    private _created_at;
    private _updated_at;
    get id(): number;
    set id(value: number);
    get uuid(): UUID;
    set uuid(value: UUID);
    get public(): boolean;
    set public(value: boolean);
    get name(): string;
    set name(value: string);
    get description(): string | null;
    set description(value: string | null);
    get location_id(): number;
    set location_id(value: number);
    get fqdn(): string;
    set fqdn(value: string);
    get scheme(): string;
    set scheme(value: string);
    get behind_proxy(): boolean;
    set behind_proxy(value: boolean);
    get memory(): number;
    set memory(value: number);
    get memory_overallocate(): number;
    set memory_overallocate(value: number);
    get disk(): number;
    set disk(value: number);
    get disk_overallocate(): number;
    set disk_overallocate(value: number);
    get upload_size(): number;
    set upload_size(value: number);
    get daemon_listen(): number;
    set daemon_listen(value: number);
    get daemon_sftp(): number;
    set daemon_sftp(value: number);
    get daemon_base(): string;
    set daemon_base(value: string);
    get created_at(): string;
    set created_at(value: string);
    get updated_at(): string;
    set updated_at(value: string);
    constructor(id: number, uuid: UUID, publicFlag: boolean, name: string, description: string | null, locationId: number, fqdn: string, scheme: string, behindProxy: boolean, memory: number, memoryOverallocate: number, disk: number, diskOverallocate: number, uploadSize: number, daemonListen: number, daemonSftp: number, daemonBase: string, createdAt: string, updatedAt: string);
}
